name = "openrouter-test"
description = "Test agent that calls openrouter.vlinder.local and returns the response"
runtime = "container"
executable = "localhost/openrouter-test:latest"

[requirements]

[requirements.models]
inference_model = "claude-sonnet"

[requirements.services.infer]
provider = "openrouter"
protocol = "openai"
