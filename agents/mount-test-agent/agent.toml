name = "mount-test-agent"
description = "Test agent with explicit mounts"
code = "agent.wasm"

[requirements]
models = []
services = []

[[mounts]]
host_path = "data"
guest_path = "/data"
mode = "ro"

[[mounts]]
host_path = "output"
guest_path = "/output"
mode = "rw"
