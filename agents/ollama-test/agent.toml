name = "ollama-test"
description = "Test agent that exercises all three Ollama endpoints via ollama.vlinder.local"
runtime = "container"
executable = "localhost/ollama-test:latest"

[requirements]

[requirements.models]
inference_model = "phi3"

[requirements.services.infer]
provider = "ollama"
protocol = "openai"
