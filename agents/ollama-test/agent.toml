name = "ollama-test"
description = "Test agent that exercises all four Ollama endpoints via ollama.vlinder.local"
runtime = "container"
executable = "localhost/ollama-test:latest"

[requirements]

[requirements.models]
inference_model = "phi3"
embedding_model = "nomic-embed"

[requirements.services.infer]
provider = "ollama"
protocol = "openai"

[requirements.services.embed]
provider = "ollama"
protocol = "openai"
