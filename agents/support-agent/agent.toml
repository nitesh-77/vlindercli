name = "support"
description = "Triage orchestrator that delegates to log-analyst and code-analyst, then classifies responses into five categories: misconfiguration, bug, feature request, author guide, or out of scope."
runtime = "container"
executable = "localhost/vlinder-support:latest"

[requirements]
services = ["infer"]

[requirements.models]
default = "ollama://localhost:11434/phi3:latest"
