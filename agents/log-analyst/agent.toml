name = "log-analyst"
description = "Runtime behavior specialist that searches vlinder logs for relevant entries, correlates timestamps, and identifies error patterns."
runtime = "container"
executable = "localhost/vlinder-log-analyst:latest"

[requirements]

[[mounts]]
host_path = "~/.vlinder"
guest_path = "/vlinder"
mode = "ro"
