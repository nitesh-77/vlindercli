name = "missing-mount-agent"
description = "Test agent with mount that doesn't exist"
runtime = "container"
executable = "localhost/missing-mount-agent:latest"

[requirements]
services = []

[[mounts]]
host_path = "nonexistent"
guest_path = "/data"
mode = "ro"
