name = "council-orchestrator"
description = "Orchestrates a council of three advisors over multiple deliberation rounds, seeking consensus on what vlindercli should build next."
runtime = "container"
executable = "localhost/council-orchestrator:latest"

[requirements]

[requirements.models]
default = "gemini-flash"

[requirements.services.infer]
provider = "openrouter"
protocol = "openai"
